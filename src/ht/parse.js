export class Lexer {
  
  constructor(text) {
    this.text = text;
  }
  
  scan() {
    this.index = 0;
    this.tokens = [];
    
    while (this.index < this.text.length) {
      
    }
  }
  
}

export class Parser {
  
}